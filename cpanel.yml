---
deployment:
  tasks:
    - export DEPLOYPATH=/home/instants/repositories/Api_Countries
    - /bin/cp src/ $DEPLOYPATH
    - /bin/cp package.json $DEPLOYPATH
    - /bin/cp index.js $DEPLOYPATH
    - /bin/cp server.js $DEPLOYPATH
